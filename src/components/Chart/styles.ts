import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles({
  indicatorVisible: {
    display: 'block',
  },
  indicatorInvisible: {
    display: 'none',
  },
});

export default styles;
